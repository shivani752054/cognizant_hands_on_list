import { Component } from '@angular/core';import { NotificationService } from './notification.service';@Component({standalone:true,providers:[NotificationService],template:`<p>Notification service is component-scoped.</p>`})export class NotificationComponent{constructor(public notifications:NotificationService){} /* providers creates an isolated instance for this component subtree. */}