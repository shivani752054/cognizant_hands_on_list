import { TestBed } from '@angular/core/testing';import { provideHttpClient } from '@angular/common/http';import { provideHttpClientTesting,HttpTestingController } from '@angular/common/http/testing';import { CourseService } from './course.service';
describe('CourseService',()=>{let s:CourseService,h:HttpTestingController;beforeEach(()=>{TestBed.configureTestingModule({providers:[CourseService,provideHttpClient(),provideHttpClientTesting()]});s=TestBed.inject(CourseService);h=TestBed.inject(HttpTestingController);});afterEach(()=>h.verify());it('gets courses',()=>{const mock=[{id:1,name:'A',code:'A1',credits:3,gradeStatus:'passed' as const},{id:2,name:'B',code:'B1',credits:4,gradeStatus:'pending' as const}];s.getCourses().subscribe(c=>expect(c.length).toBe(2));h.expectOne('http://localhost:3000/courses').flush(mock);});});