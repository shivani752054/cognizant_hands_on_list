import { inject } from '@angular/core'; import { HttpInterceptorFn } from '@angular/common/http'; import { finalize } from 'rxjs'; import { LoadingService } from './loading.service';
export const loadingInterceptor:HttpInterceptorFn=(req,next)=>{const l=inject(LoadingService);l.isLoading$.next(true);return next(req).pipe(finalize(()=>l.isLoading$.next(false)));};